
#include "VEDLib.h"


void RotX (double a);
double DEFORMATION [2][2] = {{50, 0},
                             {0, 50}}

MYP MYSDEF = {2, 2};

int main()
    {
    txCreateWindow (800, 600);

    double cord [2][2] = {{1, 1}, {0, 0}};
    
    while (!GetAsyncKeyState (VK_ESCAPE))
        {
        if (GetAsyncKeyState ('A')) RotX (-0.1);
        if (GetAsyncKeyState ('D')) 

    return 0;
    }




void RotX (double a)
    {
    double rotation[2][2] = {{cos(a), -sin(a)},
                             {sin(a),  cos(a)}};
    multimat (MYSDEF, DEFORMATION, MYSDEF, rotation, MYSDEF, DEFORMATION);
    }
    